#ifndef SUM_ALGO
#define SUM_ALGO

#include <vector>
#include <iostream>

std::vector<int> twoSum (std::vector<int>& nums, int, int);
std::vector<int> threeSum (std::vector<int>& nums, int, int);
std::vector<int> threeSumCloset (std::vector<int>& nums, int, int);

#endif